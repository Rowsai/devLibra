# View DoT Icons — devLibra v0.0.0.28

## 使用方法

1. devLibraを無効化し、今回の `latest.zip` を展開してDLLなどを更新します。
2. devLibraを有効にして `/devlibra` を開きます。
3. `View DoT Icons` タブの「有効」をチェックします（初期値はOFF）。
4. 敵へ蠱毒法やディアなどを付与すると、表示中のネームプレートの上にステータスアイコンと残り秒数を表示します。

`/dl show dot` は同じ有効設定をON/OFF切り替えし、結果をローカルチャットに表示します。設定は自動保存し、再読み込み後も保持します。タブには現在の表示状態とコマンドの使用可否を表示します。別のプラグインが `/dl` を登録済みの場合は競合を表示し、チェックボックスによる操作は引き続き可能です。

## サイズ・位置設定

「数値サイズ」スライダーで残り秒数の文字サイズを8～48pxに設定できます（初期値16px）。変更は即座に反映して自動保存します。アイコン内に収まらない大きさの場合は、設定値を上限に自動縮小します。大きな数値を表示したい場合はアイコンサイズも大きくしてください。

View DoT Icons タブで、アイコンサイズを16～64pxのスライダーで変更できます（初期値32px）。表示位置は上・右・左の3種類です（初期値は上）。変更は即座に反映して自動保存します。サイズはHUD倍率に連動せず、指定したピクセル数を使用します。

上はネームプレート中央揃え、右・左はネームプレートの外側に縦方向中央揃えで表示します。複数のアイコンは、どの位置でも横並びです。残り秒数は小さいアイコンでも内側に収まるよう縮小します。

## 表示仕様

- 自分のEntityIdとステータスのSourceIdが一致するDoTのみ表示します。他プレイヤーや別EntityIdのペットが付与したものは対象外です。
- 複数のDoTは横並びにし、ステータスID順で固定します。
- 残り時間は秒単位で切り上げます（29.2秒→30、0.1秒→1）。数字はアイコン内の中央に表示し、5秒以下で色を変えます。
- ゲームの残り時間を毎フレーム参照するため、再付与時の延長にも追従します。
- 解除・時間切れ・敵の死亡・ネームプレート非表示・ログアウト・機能OFFで表示を消します。
- 敵のネームプレートが非表示の設定になっている場合、DoTアイコンも表示しません。
- ゲームのネームプレート位置を参照するクリック透過のオーバーレイです。ゲームのステータスやネームプレートのノードを書き換えません。

## DoTの分類

クライアント内の英語Statusシートから、デバフ分類で、説明文に継続ダメージを受ける内容（sustaining / causing等とdamage over time）があるステータスを抽出します。表示アイコンは同じゲームデータのIconを使用します。日本語クライアントでも判定に使うシートは英語なので、表示言語には依存しません。動作中の外部APIへの通信はありません。

蠱毒法（1895）とディア（1871）が読み込めたことを起動時に確認します。読み込み失敗時はタブにエラーを表示します。特殊な説明文を持つDoTや、ステータスとして敵に付与されない設置型ダメージは表示対象にならない場合があります。

## 検証状況

- API15 / .NET 10でReleaseビルド成功。既存由来の警告5件、追加コードの警告0件。
- DoT分類、他人のDoT除外、解除・不正な残り時間の除外、秒数表示、コマンド解析の42アサーションが成功。
- 既存ソート機能の73,964アサーションも成功。
- ゲーム内の描画は未検証です。アイコン位置、HUD倍率、カメラ移動、複数の敵、DoT再付与、ネームプレート表示設定による消去、チェックボックスとコマンドの連動を実機で確認してください。

## ソース

- `EnemyDotNameplateDisplay.cs`: カタログ読込・コマンド・ネームプレート位置の取得と描画。
- `DotDisplayRules.cs`: DoT判定、付与者と残り時間の検証、秒数文字列。
- `Configuration.cs`: 保存設定 `ViewDotIconsEnabled`。
- `Windows/MainWindow.cs`: 新しいView DoT Iconsタブ。
- `tests/DotRulesTests.cs`: DoTのロジックテスト。

構造の参照: [AddonNamePlate](https://github.com/aers/FFXIVClientStructs/blob/main/FFXIVClientStructs/FFXIV/Client/UI/AddonNamePlate.cs)、[NamePlateNumberArray](https://github.com/aers/FFXIVClientStructs/blob/main/FFXIVClientStructs/FFXIV/Client/UI/Arrays/NamePlateNumberArray.cs)。



## v1.0.0.0 追加対応（バージョン変更なし）

DoTに加え、自分が付与したデスデザイン（Status ID 2586）を例外として表示します。他のDoT以外のデバフは対象外です。残り時間、アイコンサイズ、数値サイズ、位置設定は共通です。他人が付与したデスデザインは表示しません。


## 残り時間の視覚効果（v1.0.0.1）

最初に観測した残り時間を基準に、経過に合わせて上から下へ暗いグラデーションを進めます。途中から表示した場合は、その時点の残り時間が基準です。時間延長を検出するとグラデーションをリセットします。残り3秒以下でアイコンの明るさを約2回/秒の周期で変化させます。秒数は読みやすいよう点滅させません。時間切れ・解除時には消去します。ゲーム内の外観は未確認です。


数値の背景矩形を廃止し、文字と影だけで表示します。残り3秒の点滅は約2回/秒を維持し、明度100%と10%を交互に切り替えます。明るい間は暗転グラデーションを弱め、オレンジ色の枠を表示します。バージョンは1.0.0.1のままです。


## 文字色・縁取りとソフトな点滅（最新仕様）

文字色と透明度を変更でき、縁取りの有効無効・色・透明度・太さ（0.5～3px）を設定できます。初期値は白文字と黒の1px縁取りです。背景矩形は表示しません。文字色は残り時間によって上書きしません。設定は即時反映・自動保存されます。

オレンジ枠を廃止しました。残り3秒以下では約1秒周期の滑らかな曲線で明度10%～100%を往復し、暗転グラデーションの濃さも滑らかに変化します。数値は点滅させません。この仕様が以前のストロボ点滅・オレンジ枠・残り5秒の文字色変更に優先します。バージョンは1.0.0.1です。

